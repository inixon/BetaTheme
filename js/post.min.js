const progressBar=document.querySelector(".reading-progress");function updateProgress(){const totalHeight=document.body.clientHeight,windowHeight=document.documentElement.clientHeight,position=window.scrollY,progress=position/(totalHeight-windowHeight)*100;progressBar.setAttribute("value",progress),requestAnimationFrame(updateProgress)}function copyTextToClipboard(){event.preventDefault(),event.stopPropagation();var copyUrl=window.location.href;navigator.clipboard.writeText(copyUrl),event.target.classList.add("done")}requestAnimationFrame(updateProgress);
